describe('Tipe Data', function() {
    it('should must declare', function() {
        
        // const adalah constant
        // tipe data primitif
        const nama: String = "Alpian";
        const number: Number = 82920383
        const boolean: Boolean = true

        // cetak ke layar
        console.info(nama);
        console.info(number);
        console.info(boolean);

    });
});